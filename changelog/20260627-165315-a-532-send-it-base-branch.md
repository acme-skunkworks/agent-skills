---
title: Make send-it's base-ref resolution honour config.json baseBranch
release_note: The send-it skill's bump-derivation helpers (derive-bump / check-skill-bumps) now diff the branch against the trunk configured in config.json (baseBranch) instead of a hardcoded origin/main. A consumer whose trunk is develop now gets the right commit range — and therefore the right version-bump signal — rather than silently diffing against origin/main. BASE_REF still overrides per-run, and origin/main remains the final fallback.
created_at: '2026-06-27T16:53:15Z'
merged_at: '2026-06-27T17:25:50Z'
branch: a-532-send-it-derive-bump-should-honour-configbasebranch-config-vs
pr: 61
commit: 40d1fa5
merge_strategy: squash
author: rob@acmeskunkworks.io
co_authors: []
category: fix
breaking: false
issues:
  - A-532
stats:
  files_changed: 5
  loc_added: 130
  loc_removed: 9
  commits: 1
version: 1.2.0
---

## Fixed

- **`send-it` base-ref resolution honours `config.json` `baseBranch` ([A-532](https://linear.app/acme-skunkworks/issue/A-532)).**
  `resolveBaseRef` hardcoded the chain `BASE_REF → origin/main → main` and never
  read `baseBranch`, so a consumer whose trunk is `develop` (with
  `baseBranch: "develop"`) still diffed against `origin/main` — yielding the wrong
  commit range and bump signal in `derive-bump` / `check-skill-bumps`. The chain now
  slots the configured trunk in ahead of the fallback (`BASE_REF → origin/<baseBranch>
  → <baseBranch> → origin/main → main`); with `baseBranch: "main"` it collapses to
  the original list, so existing repos are unaffected. The candidate-list logic is
  factored into a pure `baseRefCandidates` with unit coverage. Bundled as
  `send-it@0.3.3`.
