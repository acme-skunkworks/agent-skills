---
title: Make linear-sync and cleanup-repo build the issue-ID regex deterministically
release_note: The linear-sync and cleanup-repo skills now spell out the correct, deterministic issue-ID regex construction instead of telling the model to "join issueKeys with |". The naive join produced a precedence bug for multi-key teams (["A","B"] became \bA OR B-\d+\b, matching a bare A and missing A-7); the prose now mandates escaping each key, wrapping the alternation in (?:…), and guarding the empty case — mirroring the canonical, tested ISSUE_RE in the changelog bundle's add-links.mjs.
created_at: '2026-06-27T17:46:40Z'
merged_at: '2026-06-27T17:49:15Z'
branch: a-539-linear-syncchangelog-share-the-branchissue-key-extraction
pr: 65
commit: f8162e7
merge_strategy: squash
author: rob@acmeskunkworks.io
co_authors: []
category: fix
breaking: false
issues:
  - A-539
stats:
  files_changed: 5
  loc_added: 69
  loc_removed: 16
  commits: 1
version: 1.2.0
---

## Fixed

- **Deterministic issue-ID regex in linear-sync and cleanup-repo ([A-539](https://linear.app/acme-skunkworks/issue/A-539)).**
  Both skills left the `issueKeys`→regex to the model in prose ("join `issueKeys`
  with `|`"), which reproduces a precedence bug for multi-key teams:
  `["A","B"]` → `\bA|B-\d+\b`, i.e. `\bA` *or* `B-\d+\b` — matching a bare `A` and
  missing `A-7`. The prose now specifies the **deterministic** construction the
  changelog bundle's `add-links.mjs` (`ISSUE_RE`) already implements and tests:
  escape each key's metacharacters, wrap the alternation in `(?:…)` when there is
  more than one key (`\b(?:A|B)-\d+\b`), and match nothing when no keys are
  configured. Each skill cross-references that canonical implementation so the rule
  can't drift. (A vendored shared `lib/issue-keys.mjs` consumed by every bundle —
  the review's longer-term ideal — stays deferred pending a cross-bundle vendor/
  drift-check mechanism; the bundles remain independently installable with no
  cross-bundle imports.) Bundled as `linear-sync@0.3.1` and `cleanup-repo@0.3.1`.
