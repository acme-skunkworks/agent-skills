---
title: Enforce config/example key parity and finish the --help CLI convergence
release_note: pnpm validate:skills now asserts that every skill shipping both config.json and config.example.json keeps their key sets in sync (values may differ), so a knob added to one but not the other is caught in CI. send-it's two config files are reconciled to one key set. The triage-pr review-threads fetcher gains a --help flag, completing the estate-wide convention. The intentional baseBranch-vs-mainBranch trunk-key divergence is now documented.
created_at: '2026-06-27T17:38:01Z'
merged_at: '2026-06-27T17:40:08Z'
branch: a-538-cross-skill-uniform-help-self-test-configexample-key-parity
pr: 64
commit: a03a7ae
merge_strategy: squash
author: rob@acmeskunkworks.io
co_authors: []
category: feature
breaking: false
issues:
  - A-538
stats:
  files_changed: 11
  loc_added: 216
  loc_removed: 6
  commits: 1
version: 1.2.0
---

## Added

- **Config/example key-parity check ([A-538](https://linear.app/acme-skunkworks/issue/A-538)).**
  `pnpm validate:skills` now asserts that any `skills/<name>/` shipping **both**
  `config.json` and `config.example.json` keeps their key sets identical
  (recursively; values may differ — the example carries placeholders). A key in one
  but not the other means an adopter copying the template silently gets a stale or
  incomplete config. Skills shipping only one file (e.g. `preflight`) are exempt.
  This caught a real divergence in `send-it`, now reconciled (`config.json` and
  `config.example.json` share one key set). Bundled as `send-it@0.3.4`.
- **`review-threads.mjs` gains `--help` ([A-538](https://linear.app/acme-skunkworks/issue/A-538)).**
  The triage-pr review-thread fetcher was the last user-facing bundled script
  without a `--help` flag; it now prints usage and exits before fetching.
  (`classify-lint.mjs` is an internal module with no CLI entrypoint, so it stays
  exempt.) Bundled as `triage-pr@0.4.2`.

## Changed

- **Documented the trunk-key naming ([A-538](https://linear.app/acme-skunkworks/issue/A-538)).**
  The diff-based skills name the trunk `baseBranch` (`changelog`, `send-it`) while
  `cleanup-repo` names its merge-detection target `mainBranch`; CLAUDE.md now records
  that the divergence is intentional (`initialise-skills` maps between them), so
  neither is "fixed" to match the other.
