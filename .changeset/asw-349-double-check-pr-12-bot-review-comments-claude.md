---
---

Post-merge follow-up to PR #12 (ASW-318): close the remaining Claude review findings on the CI/test infrastructure — drop the bats cache `restore-keys` fallback, single-source the actionlint version, pre-export PATH before the yamllint install guard, and fix an inaccurate fake-npm test comment. CI/test-only; no consumer-facing change.
