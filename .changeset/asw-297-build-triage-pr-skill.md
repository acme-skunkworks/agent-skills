---
"@acme-skunkworks/agent-skills": minor
---

Add the `triage-pr` skill: drive a pull request from draft with failing CI to merge-ready — fix in-scope CI failures (never weakening CI to greenwash), then validate and action unresolved AI review feedback. Bundles a `gh`-backed review-thread fetcher with a `--self-test` suite.
