---
---

chore: initial scaffold (ASW-132). Bootstrap the agent-skills repo — Changesets setup, release/validate workflows, CLAUDE.md mirroring Octavo conventions, MIT LICENSE, placeholder `architecture/` and `skills/` trees, plus a stopgap `/send-it` cloned from `@acme-skunkworks/eslint-config` (tracked for removal in ASW-135). Empty frontmatter — nothing publishable while the root is `private: true`; the first real bump arrives with skill #1.
